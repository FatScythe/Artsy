import { CartNav } from "../cart/cart";
import "./schedule.css";
const Schedule = () => {
  return (
    <section className='schedule container'>
      <CartNav />
      <h2>Schedule</h2>
    </section>
  );
};

export default Schedule;
